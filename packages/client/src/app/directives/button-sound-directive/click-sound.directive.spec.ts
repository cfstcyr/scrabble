import { ClickSoundDirective } from './click-sound.directive';

describe('ClickSoundDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickSoundDirective();
    expect(directive).toBeTruthy();
  });
});
